command_list = {'0':'exit()',
                '1':'url = Url(user.id)',
                '2':'Url.urls_print()',
                '3':'Url.url_finder()'
                }
FILE_NAME = 'URL_Shortener.db'
MENU_DISCR = """
    System offer options:
    1. Create a new URL
    2. List all available URLs
    3. Find the URL via short URL

    0. Exit
    """